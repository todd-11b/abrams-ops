# Follow-ups after the quote moment works

Not blockers for v0.

- Per-account price book before shop #2. v0 uses the Abrams `FENCE_STYLES` const.
- Brother's live GHL vs a sandbox: same webhook. Set `GHL_API_KEY`, `GHL_LOCATION_ID`, `QUOTE_BRAIN_SECRET` on the target Vercel env.
- Managed-agent content setup (week of fence/tour posts from one prompt) is a GHL snapshot, not more app code.
- TourOps morning loop can reuse this connector pattern with different tools.
- Inventory / job P&L only after quoting-by-voice is real.
